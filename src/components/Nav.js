import React from 'react';

const NavItem = () => (
  <li />
)

const style = {
  position: 'fixed',
  top: 0,
  left: 0,
  width: '5rem',
  height: '100%',
  background: 'black'
};

const Nav = () => {
  return (
    <ul style={style}>
      
    </ul>
  );
};

export default Nav;